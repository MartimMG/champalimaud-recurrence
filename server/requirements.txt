# SurvSHAP API — install in a Python 3.10+ venv (Linux/macOS or WSL recommended).
# On Windows, scikit-survival may require MSVC build tools for some dependencies; use Docker or WSL if pip fails.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
numpy>=1.26.0
pandas>=2.0.0
survshap>=0.4.2
scikit-survival>=0.17.2
scikit-learn>=1.3.0
shap>=0.41.0
